#version 450
layout(rgba32f, binding = 0) uniform image2D imgOut;

layout (local_size_x = 32, local_size_y = 32) in;
void main() {
    ivec2 storePos = ivec2(gl_GlobalInvocationID.xy);
    imageStore(imgOut, storePos, vec4(normalize(storePos),0.0,1.0));
}